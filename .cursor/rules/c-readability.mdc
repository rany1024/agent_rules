---
description: C 可读性（嵌套深度、表达式复杂度、控制流，禁止炫技）
globs: **/*.{c,h}
alwaysApply: false
---

# C 可读性

- 代码首先服务于人类维护者，其次才是机器执行
- 无特殊要求(例如明确表示需要优化性能),优先清晰，禁止炫技（晦涩位运算、深层嵌套、复杂宏、嵌套三元运算符）
- 嵌套不超过 3 层；超过则 early return 或拆函数
- 单行只做一件事：禁止「赋值 + 判断 + 调用」混写
- 条件分支：优先 `switch` 或查表，避免长 `if-else` 链
- 不为未需求场景过度抽象；两处或以上相同逻辑再提取函数
- 寄存器/位域操作提取为命名常量或 static 函数，并注释位含义

## 示例

```c
/* BAD: nesting too deep */
if (a) {
    if (b) {
        if (c) {
            do_work();
        }
    }
}

/* GOOD: early return */
if (!a || !b || !c) {
    return ERR_INVALID;
}
do_work();
```
