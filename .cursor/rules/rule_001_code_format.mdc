---
description: C 代码编码风格规范（函数花括号、空格、变量声明、可读性）
globs: **/*.{c,h}
alwaysApply: false
---

# C 编码风格

在 C 代码上严格遵守以下编码风格。

## 规则

- 函数左花括号放在第二行（Allman 风格）
- `if` / `for` 的 `(` 前要加空格；除了函数外的其它情况花括号放在前缀行末
- 变量在靠近使用的地方声明，并带有初始值
- 代码逻辑块之间用空行隔开
- 原则上函数不应过大，最好不超过一页
- 除非特殊情况（例如性能优化、需要大量篇幅减少循环引用等），优先保证易读性
- 复杂逻辑用易懂的函数名封装

## 示例

```c
void function(void param)  /* 函数花括号在第二行 */
{
    if (a < b) {  /* if/for 在 ( 前要加空格，花括号在行末 */
    }

    int i = 0;  /* 变量靠近使用处声明，并带初始值 */
    for (i = 0; i < 100; i++) {
    }

    /* 逻辑块之间空行隔开 */
    while (1) {
    }
}
```

