---
description: C 代码格式（Allman 花括号、空格、函数长度、s_ 命名）
globs: **/*.{c,h}
alwaysApply: false
---

# C 代码格式

- 函数左花括号放在第二行（Allman 风格）
- `if` / `for` / `while` 等控制语句：`(` 前加空格，左花括号放在同一行末尾
- 变量在靠近使用处声明，并尽量赋予初始值
- 代码逻辑块之间用空行隔开
- 函数长度：超过 40 行即考虑拆分，硬上限 80～100 行
- 除非特殊情况（如性能优化、循环展开等），优先保证代码易读
- 复杂逻辑用含义清晰的函数名封装
- 文件内 static 变量以 `s_` 为前缀（如 `s_info`）

## 示例

> 下面行尾注释仅用于讲解规则，实际代码不要逐行加这类复述性注释（见 `c-comments`）。

```c
void function(int param)
{
    if (a < b) {
    }

    int i = 0;
    for (i = 0; i < RET_MAX; i++) {
    }

    while (1) {
    }
}
```
