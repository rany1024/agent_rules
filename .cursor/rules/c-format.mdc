---
description: C 代码格式（Allman 花括号、空格、函数长度、s_ 命名）
globs: **/*.{c,h}
alwaysApply: false
---

# C 代码格式

- 函数左花括号放在第二行（Allman 风格）
- `if` / `for` / `while` 等控制语句：`(` 前加空格，左花括号放在同一行末尾
- 变量在靠近使用处声明，并尽量赋予初始值
- 代码逻辑块之间用空行隔开
- 原则上单函数不宜超过 80～100 行，超过则拆分
- 除非特殊情况（如性能优化、循环展开等），优先保证代码易读
- 复杂逻辑用含义清晰的函数名封装
- 文件内 static 变量以 `s_` 为前缀（如 `s_info`）

## 示例

```c
void function(int param)  /* function brace on next line (Allman) */
{
    if (a < b) {  /* control stmt: space before (, brace on same line */
    }

    int i = 0;  /* declare near use, with initializer */
    for (i = 0; i < RET_MAX; i++) {
    }

    /* blank line between logic blocks */
    while (1) {
    }
}
```
