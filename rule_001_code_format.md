在c代码上严格遵守以下编码风格:
```
void function(void param) // 函数花括号在第二行
{
    if (a < b) { // if for 在(前要加空格 其它情况花括号在前缀行末
    }
    
    int i = 0;// 变量在靠近使用的地方声明, 并带有初始值
    for (i = 0; i < 100; i++) {
    }
    // 代码逻辑之前空行隔开, 原则上函数不应过大, 最好不超过一页
    // 除非特殊情况(例如性能优化 需要大量篇幅减少循环引用等)
    // 尽量保证代码易读性, 逻辑清晰, 对于复杂逻辑, 用易懂的函数名称封装起来
    while (1) {
    }

}
```
